import { UpdatePasswordForm } from '@/components/auth/password-reset-forms';
export const metadata = { title: 'Choose a new password — IP Sports OS' };
export default function ResetPasswordPage() { return <UpdatePasswordForm />; }
