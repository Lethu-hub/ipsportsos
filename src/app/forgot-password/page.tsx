import { ForgotPasswordForm } from '@/components/auth/password-reset-forms';
export const metadata = { title: 'Reset password — IP Sports OS' };
export default function ForgotPasswordPage() { return <ForgotPasswordForm />; }
